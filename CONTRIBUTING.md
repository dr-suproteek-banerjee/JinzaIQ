# Contributing

Keep changes small, typed, tested, and documented.

- Backend domain logic belongs in `backend/app/services`.
- API route handlers should validate, authorize, and delegate.
- AI prompts live under `backend/app/ai/prompts`.
- Frontend pages should stay focused and move reusable UI into `frontend/components`.
- Avoid adding real scraped job data unless the source permits it.
